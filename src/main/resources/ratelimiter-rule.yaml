configs:
  - appId: app-1
    limits:
      - api: /v1/user
        limit: 1
      - api: /v1/order
        limit: 1
  - appId: app-2
    limits:
      - api: /v1/user
        limit: 50
      - api: /v1/order
        limit: 50